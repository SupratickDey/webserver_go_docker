# Docker Compose file Reference (https://docs.docker.com/compose/compose-file/)
version: "3"
services:
  # App Service
  license:
    container_name: ezrx_license_api_uat
    build:
      context: ./
      dockerfile: dockerfile
    ports:
      - "6547:6547"
    restart: unless-stopped
